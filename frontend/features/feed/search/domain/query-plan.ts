/**
 * 질의 해석 결과 — LLM이 읽은 부정과 의도 (부정 조각 3단계).
 *
 * 프레임워크·fetch에 의존하지 않는 순수 로직이라 domain에 둔다. 라우트 핸들러와
 * 테스트가 같은 것을 쓴다.
 */

/**
 * 프롬프트 버전. **프롬프트를 고치면 반드시 올린다.**
 *
 * 캐시 키에 들어간다 — 옛 해석은 다른 규칙으로 만들어진 것이라 그대로 쓰면 안 된다.
 * 올리지 않으면 프롬프트를 고쳐도 캐시가 옛 답을 계속 준다.
 */
export const PROMPT_VERSION = 7;

/**
 * 부정으로 뺄 수 있는 것 — **닫힌 집합**.
 *
 * ⚠️ 이 목록은 DB의 `c_search_negation_terms`와 같아야 한다. 서버가 그 표로 다시
 * 검증하므로, 여기에만 있는 값을 넣으면 캐시 쓰기가 통째로 거절된다(설계 130행 —
 * LLM은 값만 고르고 종류는 표가 정한다).
 */
export const EXCLUDABLE = [
  "로고",
  "프린트",
  "프린팅",
  "그래픽",
  "레터링",
  "자수",
  "브이넥",
  "비침",
  "얇음",
  "두꺼움",
  "슬림핏",
  "신축",
] as const;

/**
 * 의도를 옮길 수 있는 낱말 — 카탈로그에 **실제로 있는 것만**.
 *
 * 기준은 `c_search_docs.doc`에서 **500건 이상**이다. 검색이 보는 것은 `doc`이지
 * `tags`나 `c_goods`의 열이 아니다 — 이걸 헷갈리면 표에는 많은데 검색에는 없는 말을
 * 넣게 된다. 실제로 `레귤러`는 `c_goods.fit`에 19,452건이지만 `doc`에는 1,087건뿐이다.
 *
 * 실측으로 **기각한 것**(2026-08-18): 드롭숄더 128 · 박시 157 · 여름 292.
 * `기본`(366)은 이 선 아래지만, 체형 변경의 효과를 흐리지 않으려고 이번엔 두었다 —
 * 다음에 손댈 때 뺀다.
 */
const EXPANDABLE = [
  // 활동·기능
  "골프",
  "러닝",
  "요가",
  "스포츠",
  "등산",
  "쿨링",
  "냉감",
  "드라이",
  // 핏·기장 (체형 해석이 쓴다)
  "오버핏",
  "루즈",
  "레귤러",
  "슬림",
  "머슬",
  "크롭",
  // 넥라인
  "크루넥",
  "보트넥",
  "라운드넥",
  // 소재·두께
  "코튼",
  "헤비",
  // 그 밖
  "무지",
  "기본",
  // ⚠️ '여성'을 넣지 않는다. 확장어는 질의 문자열에 덧붙는데, 성별어는 서버가
  // 하드 조건으로 해석한다(20260820700000) — LLM 추론이 조용히 성별 필터가 된다.
  // 사용자가 직접 친 성별어만 조건이 되어야 한다(사람이 정함, 2026-08-20).
] as const;

/** 색 코드 — 부정에 쓸 수 있는 것만 (c_color_groups의 대표 색군) */
const NEGATABLE_COLORS: Record<string, string> = {
  "1": "화이트",
  "2": "블랙",
  "3": "그레이",
  "4": "브라운",
  "5": "베이지",
  "6": "그린",
  "7": "블루",
  "8": "퍼플",
  "9": "옐로우",
  "10": "핑크",
  "11": "레드",
  "12": "오렌지",
  "36": "네이비",
  "49": "버건디",
  "79": "라임",
};

/**
 * 실측 기반 핏 규칙 — **닫힌 집합**.
 *
 * 값은 DB의 `c_search_fit_rules`와 같아야 한다. 규칙 이름은 사용자가 **원하는 것**이고,
 * 서버가 이것을 `짧은소매` 같은 **아는 위반**으로 옮겨 `exclude`에 넣는다. LLM에게
 * "원한다"와 "빼달라"를 뒤섞어 말하게 하면 틀리므로 필드를 나눈다.
 */
export const FIT_RULES = ["팔뚝살커버", "어깨넓어보임"] as const;

export interface QueryPlan {
  /** 뺄 것 — EXCLUDABLE 안의 값만 */
  exclude: string[];
  /** 뺄 색의 코드 */
  exclude_colors: string[];
  /** 의도를 옮긴 낱말 — 하드 조건이 아니라 텍스트 점수에만 얹힌다 */
  expand: string[];
  /** 실측 핏 규칙 — 서버가 `exclude`로 옮긴다. 클라이언트는 그대로 쓰지 않는다 */
  fit: string[];
}

export function emptyPlan(): QueryPlan {
  return { exclude: [], exclude_colors: [], expand: [], fit: [] };
}

/**
 * 키워드 질의인가 — 그러면 LLM을 타지 않는다 (설계 S-02).
 *
 * 기준은 C단계에서 사람이 정한 **"2단어 초과일 때만"** 이다. `검정 반팔`처럼 규칙이
 * 이미 잘 처리하는 것에 1~2초와 돈을 쓸 이유가 없다.
 */
export function isKeywordQuery(query: string): boolean {
  return query.trim().split(/\s+/).filter(Boolean).length <= 2;
}

/**
 * LLM 출력을 읽는다. **닫힌 집합을 벗어난 값은 버린다.**
 *
 * ⚠️ 서버(`c_search_plan_put`)는 벗어난 값이 하나라도 있으면 **통째로** 거절한다.
 * 여기서 걸러 두면 LLM이 하나 헛디뎠다고 해석 전체를 잃지 않는다. 둘 다 필요하다 —
 * 여기는 관대하게 줍고, 저기는 엄격하게 막는다.
 */
export function parsePlan(text: string): QueryPlan | null {
  let raw: unknown;
  try {
    raw = JSON.parse(text);
  } catch {
    return null;
  }
  if (typeof raw !== "object" || raw === null) return null;
  const obj = raw as Record<string, unknown>;

  const pick = (key: string, allowed: readonly string[] | null): string[] => {
    const v = obj[key];
    if (!Array.isArray(v)) return [];
    const out = v.filter(
      (x): x is string =>
        typeof x === "string" && (allowed === null || allowed.includes(x)),
    );
    return [...new Set(out)];
  };

  return {
    exclude: pick("exclude", EXCLUDABLE),
    exclude_colors: pick("exclude_colors", Object.keys(NEGATABLE_COLORS)),
    // 확장어는 개수 상한이 서버에도 있다(8개). 여기서 먼저 자른다.
    expand: pick("expand", EXPANDABLE).slice(0, 8),
    fit: pick("fit", FIT_RULES),
  };
}

/** LLM에게 주는 지침. 고치면 `PROMPT_VERSION`을 올려야 한다. */
export const INTERPRET_PROMPT = `너는 티셔츠 쇼핑 검색의 질의 해석기다. 사용자 질의를 읽고 **아래 목록에서만** 골라 JSON을 낸다.

## 낼 것 (JSON만, 다른 말 금지)
{"exclude": [], "exclude_colors": [], "expand": [], "fit": []}

## exclude — 사용자가 "없는/말고/않은"으로 **빼달라고 한 것**만
고를 수 있는 값은 이것뿐이다. 다른 말을 지어내지 마라:
${EXCLUDABLE.join(", ")}

## exclude_colors — 사용자가 빼달라고 한 **색의 코드**
${Object.entries(NEGATABLE_COLORS)
  .map(([code, name]) => `${code}=${name}`)
  .join(" ")}

## expand — 질의의 **의도**를 상품 낱말로 옮긴 것 (최대 8개)
카탈로그에 실제로 있는 말만 쓴다. 쓸 수 있는 값:
${EXPANDABLE.join(", ")}

**상황·활동은 기능 낱말로 옮겨라.** 이게 이 필드의 핵심 쓸모다. 사용자는 상품 속성이
아니라 자기 상황을 말한다:
- "여름에 시원한", "더울 때" → 쿨링, **냉감**, 드라이 (셋은 같은 뜻의 다른 표기다.
  하나만 고르지 마라 — 카탈로그가 셋을 다르게 적는다)
- "싸이 흠뻑쇼 갈 때", "물놀이", "여름 축제" → 쿨링, 냉감, 드라이
- "골프칠 때", "러닝할 때", "등산" → 그 종목 이름 + 쿨링·냉감·드라이
- "면 100%", "순면" → 코튼
- "회사에 입고갈", "깔끔한" → 기본, 무지, 레귤러

⚠️ 아래 「규칙」의 "근거 없으면 빈 배열"은 **상품 속성**(색·핏·소재)에 대한 말이다.
상황에서 기능을 읽어내는 것은 추측이 아니라 이 필드가 하라는 일이다.

## fit — 실측(어깨너비·소매길이)으로 판정하는 규칙
쓸 수 있는 값은 둘뿐이다:
- **팔뚝살커버** — 팔뚝을 가리고 싶다는 뜻이 있을 때. 예: "팔뚝살 커버되는", "팔뚝 가려주는",
  "이두가 신경 쓰이는", "팔이 굵어서 고민"
- **어깨넓어보임** — 어깨를 넓어 보이게 하고 싶을 때. 예: "어깨 넓어 보이는",
  "어깨 좁은 사람", "왜소해 보여서"

주의: "어깨 좁아 보이고 싶다"는 **어깨넓어보임이 아니다.** 반대 방향은 빈 배열로 둔다.

## 골격 체형 — "웨이브/스트레이트/내추럴"이 나오면 아래 표대로 옮긴다
이건 취향이 아니라 **정해진 매핑**이다. 다르게 해석하지 마라.

- **스트레이트** — 잘 만든 기본 티셔츠가 가장 잘 맞는다. 어깨선이 실제 어깨에 맞고
  몸통이 일자로 떨어지는 것. **남녀 같다.**
  → expand: 레귤러, 무지, 코튼
  (아주 얇은 것과 큰 오버핏 둘 다 아니다 — 넣지 마라)
- **내추럴** — 뼈대·관절의 존재감이 있어 옷에도 볼륨이 있어야 균형이 맞는다. **남녀 같다.**
  → expand: 오버핏, 루즈, 헤비
  (몸에 붙는 얇은 반팔은 골격이 더 도드라진다 — 넣지 마라)
- **웨이브 — ⚠️ 남녀가 정반대다. 성별 단서를 반드시 먼저 보라.**
  - **여성 웨이브** (여자·여성·she 등 단서): 상체가 얇고 무게중심이 아래. 상체를 작고
    정돈되게 보이게 한다.
    → expand: 슬림, 크롭, 크루넥, 보트넥, 코튼
    (헤비웨이트·오버핏은 몸이 옷에 묻혀 보인다)
  - **남성 웨이브** (남자·남성 등 단서): 너무 슬림하면 오히려 **왜소해 보인다.**
    어깨를 살짝 넓혀 준다.
    → fit: 어깨넓어보임 / expand: 크루넥, 코튼
    (슬림을 넣지 마라. 극단적 오버핏도 아니다)
  - **성별 단서가 없으면 웨이브는 빈 배열로 둔다.** 절반은 정반대를 받게 되므로
    안 주는 편이 낫다. 스트레이트·내추럴은 단서가 없어도 그대로 적용한다.

체형 이름이 **없으면 이 표를 쓰지 마라.** "팔뚝살"·"상체통통"처럼 체형 이름이 아닌
고민은 여기서 다루지 않는다 — 근거가 없으면 빈 배열이다.

## 규칙
- **근거가 없으면 빈 배열**로 둔다. 추측하지 마라.
- 확장어는 **많을수록 좋은 게 아니다.** 근거가 뚜렷한 것만 넣어라 — 관계가 옅은 말을
  얹으면 엉뚱한 상품이 점수를 얻는다.
- 사용자가 **원한다고** 한 것을 exclude에 넣지 마라. exclude는 **빼달라고 한 것**뿐이다.
- 색·카테고리·브랜드·가격은 다른 곳에서 이미 처리한다. expand에 넣지 마라.
- JSON 외에는 한 글자도 쓰지 마라.`;
