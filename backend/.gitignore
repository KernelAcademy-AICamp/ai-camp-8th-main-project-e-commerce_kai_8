# 가상환경·캐시
venv/
__pycache__/
.pytest_cache/

# 시크릿 (템플릿만 커밋)
.env*
!.env.example

# 로컬 백업 (커밋 금지)
backups/

# 큐레이션 목업 — gen_curation_page.py가 매번 다시 만든다 (정본은 curations 표)
scripts/큐레이션화면.html

# Supabase CLI 로컬 상태
supabase/.temp/
embed/data/
