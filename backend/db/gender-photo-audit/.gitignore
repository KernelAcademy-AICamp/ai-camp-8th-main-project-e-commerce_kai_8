# 재생성 가능한 중간 산출물은 담지 않는다 (합계 17MB).
#   manifest_*.csv  시트 칸번호 ↔ 상품번호 대조표 — build_phase.py가 다시 만든다.
#   census_*.csv    시트 입력 목록 — p_female 채점 쿼리로 다시 만든다.
# 사람 판정은 verdicts_exceptions.csv에 **상품번호로 풀어서** 담았으므로
# 이 파일들이 없어도 판정 기록을 읽을 수 있다.
manifest_*.csv
census_*.csv
