# 계획 — 개인화 3차: 선행 버그 수정 + 분류 교정 + 색군 축 + 신선도 가산점

> 상태: 확정 (2026-08-16 제품 책임자 승인 — "확정") · 작성: 2026-08-16
> 근거: [개인화 3차 설계(확정, Codex 리뷰 12건 반영)](../superpowers/specs/2026-08-16-personalization-refinement-design.md) · [2차 계획 실행 기록](2026-08-16-personalization-signals-profile-mix.md)
> 선행 결정(2026-08-16 제품 책임자): 컴퓨트 업그레이드 보류(이번엔 소프트웨어만) · 재분류 검증은 AI 자체 검증만 · 설계 v2 확정.

## 배경 (이 repo를 모르는 사람을 위한 설명)

aTee는 티셔츠 22.6만 개(이미지 96.7만 장, SigLIP2 벡터)를 2열 무한 피드로 탐색하는 PWA다. 2차까지로 행동 신호 수집·취향 프로필·5유형 포트폴리오 믹스 추천이 동작한다. 이번 3차는 2차가 남긴 후속 3건과, 외부 리뷰(Codex)로 확인된 기존 버그 2건을 처리한다:

- **버그 ①** 추천 재정렬에서 앵커 가중치가 의도와 반대로 작용(높은 가중 앵커의 후보가 뒤로 밀림).
- **버그 ②** 부적합 이미지(디테일·원단 등) 제외 조건이 세션·장기 슬롯에만 있고 부분 일치·반대·다양성 슬롯과 기본 피드에는 없음.
- **후속 A** 이미지 유형 분류에서 "표·라벨" 클래스가 전부 오분류(실제론 뒷모습 착용샷 6.1만 장) — 프롬프트를 고쳐 로컬 벡터로 재분류(재임베딩 불필요).
- **후속 B** 부분 일치·반대 제안 축이 임시 구현(성별+가격 / 그래픽 반전) — 설계 원안대로 색군 기반으로 고도화.
- **후속 C** 신선도가 계측 플래그만 있고 노출 우대(가산점)는 미구현.

## 제약 (설계에서 옮김)

- 추천·유사 RPC의 입출력 시그니처는 바꾸지 않는다(프론트엔드 원칙적 무변경, 폴백·계측 경로 유지).
- Supabase는 Micro(RAM 1GB) 유지 전제. 벡터 후보 생성(ANN)의 정렬식은 손대지 않는다(인덱스 무력화 금지).
- 재분류의 엄격 필터 전환은 평가 게이트를 통과한 경우에만. 실패 시 현행 유지(정상 상품을 다시 잃는 회귀 금지).
- 페이지 불변성(같은 시드 → 같은 페이지) 유지. 서버 갱신 작업은 배치·멱등·재개 가능.
- 품질 게이트: `frontend/`는 `npm run check`+vitest, `backend/`는 pytest. 커밋·PR은 제품 책임자 요청 시에만.

## 단계와 완료 기준

### 0. 선행 버그 수정 — 가중치 역전 + 전 경로 이미지 필터

추천 RPC의 세션·장기 재정렬에서 앵커 가중치가 유리하게 작용하도록 수식을 교정하고, 현행 이미지 제외 자격(디테일·원단 제외)을 부분 일치·반대·다양성 슬롯과 기본·폴백 피드에도 일관 적용한다(적용 위치는 성능 실측으로 결정). 성능이 기존 응답 시간에서 유의미하게 나빠지지 않는지 확인한다.

- **완료 기준:** 가중치가 다른 두 앵커로 추천을 요청하면 높은 가중 앵커 계열 상품이 결과 상위에 더 많이 보인다(교정 전후 비교 수치를 실행 기록에 남김). 부분 일치·반대·다양성·폴백 각 경로의 응답 표본에서 제외 대상 유형 이미지가 0건이다. 웜 응답 시간이 기존 범위(1.3~2.8초) 대비 악화 20% 이내다.

### 1. A — 표·라벨 프롬프트 실험·재분류 (로컬)

분류 버전(모델 리비전 고정·전처리·프롬프트 세트·라벨 병합 규칙)을 정의하고, 로컬 벡터로 프롬프트 후보들을 재스코어해 비교한다. 평가는 새로 만든다: AI가 정답 라벨을 부여한 층화 홀드아웃 표본(프롬프트 선택용 표본과 분리)과 진짜 표·라벨 양성 표본으로 정밀도·재현율을 산출하고, 합격선(표본 크기 포함)을 이 문서 실행 기록에 확정해 적는다. 기존 신뢰도·임계값은 재사용하지 않는다.

- **완료 기준:** 홀드아웃 평가표(클래스별 정밀도·재현율·전환 행렬)가 실행 기록에 적히고, 합격선 통과 여부가 명시된다. AI가 새 3류 고신뢰·경계 표본과 기존 오분류(뒷모습) 표본 이미지를 직접 확인한 결과가 기록된다.

### 2. A — 서버 반영 + 필터 강화 (게이트 통과 시)

서버 벡터 테이블에 분류 버전 열을 추가하고, 로컬 재분류 결과(라벨·신뢰도·버전)를 배치로 갱신한다(체크포인트 재개 가능, 갱신 후 행 수·클래스 분포를 로컬과 대조, 통계 갱신). 1단계 게이트를 통과했으면 노출 자격을 표·라벨 제외까지 강화하는 마이그레이션을 별도로 적용한다. 통과하지 못했으면 현행 자격을 유지하고 원인·후속을 기록한다.

- **완료 기준:** 서버에서 분류 버전별 행 수를 SQL로 조회하면 로컬 집계와 일치한다. (게이트 통과 시) 상세 하단 유사 탐색과 피드에서 진짜 표·라벨 이미지가 보이지 않고, 뒷모습 착용샷은 정상 노출된다. 배치를 중단 후 재실행해도 결과가 같다.

### 3. B — 색군 매핑 + 부분 일치·반대 축 교체

공식 색 코드 사전(무신사 필터 메타)을 정본으로 54코드→색군 매핑 테이블(색 계열 + 무채/원색 독립 속성)을 만들고(확보 실패 시 상품명+AI 검수 대체, 근거 기록), 설계의 다중값·엣지 규칙(교집합 일치, 중간색 처리, 색 없음 제외, 조인 중복 제거)대로 부분 일치·반대 축을 교체한다. 부분 일치의 성별·가격 보조 조건 유지 여부와 반대 축 결합/교대 방식은 후보 수·체감 실측으로 정해 기록한다. 그래픽 축(무지/그래픽/레터링)의 정확도도 표본으로 확인해 기록한다.

- **완료 기준:** 54코드 전원 매핑 테스트가 통과한다. 특정 색군 상품을 찜한 뒤 추천을 받으면 부분 일치 슬롯에 같은 색군·다른 그래픽 상품이, 반대 슬롯에 축 반전 상품이 보이고, 응답 표본 SQL 검사에서 각 슬롯이 축 조건을 만족한다. 유형 분포(5유형 비율)가 유지된다.

### 4. C — 신선도 가산점

이미 좁혀진 후보의 재정렬 단계에만 신선도 우대를 넣는다(ANN 정렬식 불변 — 실행 계획으로 이진 인덱스 사용 확인). 무작위 계열은 전면 우대 금지, 시드 결정적인 소폭 가중으로 구현하고 상수는 분리한다. 노출 이벤트의 정책 필드에 알고리즘 버전 태그를 올려 배포 전후 지표를 분리한다.

- **완료 기준:** 같은 시드로 두 번 요청하면 완전히 같은 페이지가 온다. 신선도 비율 계측 SQL에서 가산점 적용 후 신선 상품 노출 비율이 적용 전보다 오르되 전면 쏠림이 아니다(수치를 실행 기록에 남김). 실행 계획에서 벡터 인덱스 사용이 확인된다.

### 5. 서버 입력 방어 + 통합 검증 · 마무리

추천 RPC에 서버 측 입력 상한(앵커·제외 배열 길이, 비유한수 거부, NULL 제거)을 넣는다. 성능 게이트(실사용급 프로필 기준: 웜 p50 악화 ≤20%·p95 ≤5초, 콜드 ≤8초, 동시 2~4 요청 무실패, temp spill 없음)를 실측하고, Orca 브라우저로 전체 흐름(찜 → 축 반영 → 신선도 → 폴백)을 확인한다. `npm run check`·vitest·pytest 통과 후 실행 기록을 남기고 develop 대상 PR을 연다.

- **완료 기준:** 비정상 입력(거대 배열·NaN 가중치)을 보내면 오류 없이 거부·정화된다. 성능 게이트 수치가 실행 기록에 적히고 전부 통과다. 기존 동작(무한 피드·스크롤 복원·뒤로 가기·상세 하단 유사 탐색·폴백)이 그대로 동작한다. PR이 열린다.

## 실행 기록

- **1단계 완료 (2026-08-16):** `backend/embed/reclassify.py` 작성. 분류 버전 2 고정 요소: SigLIP2 base @ `3f9f96cb`(HF 스냅숏), 텍스트 전처리 run_embed 동일(재현 검증: 표본 4,000장 신구 라벨 일치 99.975%·신뢰도 상관 1.0000), 프롬프트 세트 C(7프롬프트→0~3 병합: 착용샷 3종(정면·뒷모습·전신)→0, 단품컷→1, 디테일→2, 사이즈표+정보이미지→3), 신뢰도=병합 라벨 확률 합. 세트 A(문구만 교정)·B(뒷모습만 추가)와 비교: A는 저신뢰 정밀도 12/24로 탈락, B는 3류 1.87만 장 잔존으로 탈락, C 채택. **홀드아웃(선택 표본과 분리, AI 라벨): 새 3류 가중 정밀도 ≈99%**(고신뢰 24/24·저신뢰 23/24, 합격선 85%), **구조 이미지 라벨 누출 1/48**(합격선 ≤2), **노출 모집단 잔존 표·라벨 0/48**(합격선 ≤1) — **게이트 통과**. 결과: 기존 3류 6.07만 → 새 3류 1,207장(진짜 표·라벨·정보 이미지), 뒷모습·전신 착용샷 4.7만은 0류로, 뒷면 단품컷 1.2만은 1류로 구조. 부수 효과 기록: 기존 2류→0류 806장은 착용 부위 클로즈업(원단 클로즈업 아님)이라 노출 전환 수용. 로컬 `imgs.img_type2/type_conf2/classify_ver=2` 적용 완료(96.7만 행).
- **2단계 완료 (2026-08-16):** `c_img_vecs.classify_ver` 열 추가(`20260816190000`), `reclassify.py push`로 **라벨 변경 행만** 87,089건 배치 반영(5천 건 단위 커밋, 서버 ver=2 조회로 재개 — 20k 부분 실행 후 이어서 67,089건, 재실행 시 todo 0 멱등 확인). 전량 재기록을 피한 이유: 벡터 테이블 UPDATE는 non-HOT라 IVFFlat·이진 인덱스 팽창(서버 type_conf는 RPC 미사용 참고값이라 v1/v2 혼재 허용, 정본은 로컬 DB — 마이그레이션 주석에 명시). 반영 후 서버 img_type 분포가 로컬과 완전 일치(0:445,015 / 1:480,556 / 2:40,163 / 3:1,207). 게이트 통과에 따라 `20260816200000`으로 노출 자격 강화: 유사·믹스 ANN 필터 `<>2` → `in (0,1)`, card_ok에 3류 반영(부적합 썸네일 상품 9개). 검증: 유사·믹스·피드 응답에서 2·3류 이미지 누출 0건, 페이지 충족 100/100, 뒷모습 앵커(70118) 유사 30장 중 구조된 ver2 이미지 12장 정상 노출, 웜 믹스 2.1초(기존 범위). card_ok 갱신 쿼리는 상관 exists(콜드 랜덤 IO 22.6만 회)가 2분+ 걸려 단일 스캔+해시 조인으로 재작성.
- **3단계 완료 (2026-08-16):** 코드→이름 정본 확보 — 무신사 공식 필터 색 목록(54종)으로 색별 PLP를 조회해 결과 상품의 숫자 코드와 교차(53코드 일치율 대부분 95~100%, 코드 85=CLEAR는 소거법). `c_color_groups`(코드·공식 이름·색 계열 11군·무채/원색 독립 속성) 마이그레이션 `20260816210000` + 믹스 축 교체: **부분 일치** = 최고 앵커와 색군 교집합 + 썸네일 그래픽 성향 다름 + 같은 성별(가격 ±30% 제거 — 풀 실측 2.2만), **반대** = 그래픽 반전 AND 무채↔원색(결합형 풀 1.5만, 중간색 앵커는 그래픽 반전만). 앵커 색 속성은 우세(첫) 코드, 교집합은 전체 코드. 부분·반대는 shuffled 소비 상한 2만(희귀 색군 앵커의 전량 스캔 방지, 부족분 다양성 backfill). 검증: 블랙 무지 앵커 → 부분 4/4 블랙계·그래픽 다름, 반대 3/3 원색·그래픽 / 레드 그래픽 앵커 → 반대 3/3 무채·무지 / 네이비(중간색) 앵커 → 반대 = 그래픽 반전만. 유형 분포 12/6/4/3/5·결정성 유지, 웜 1.6~1.8초. **그래픽 축 표본 검증(AI)**: 무지 12/12·그래픽 10/12·레터링 11/12 (~92%, 오류는 conf 0.4대 저신뢰 집중) — 축 사용 적합. pytest: 매핑 무결성(54코드 전원·중복 없음·무채∧원색 금지)·병합 규칙 테스트 5개 추가, 백엔드 45개 통과.
- **4단계 완료 (2026-08-16):** `20260816220000` 마이그레이션. **세션·장기** = 재정렬 점수 ×1.005 신선 할인(음수 점수라 더 음수=상위). 상수 여정: 1.05·1.01은 상위 후보 유사도 간격(0.1~2%)보다 커 사실상 신선 우선 정렬(실측 15/18 쏠림) → 1.005로 근소 격차만 뒤집게 조정(실측 세션 신선 50%, 후보 자연 비율 ~35%). **부분·반대·다양성** = 결정적 신선 쿼터(슬롯/4·/4·/5, 최소 1) — 전면 우대는 금지(리뷰 지적대로 후보 충분 시 전부 올해 상품이 됨). **구현 우여곡절(잠복 취약점 2개 발견·구조 수정):** ① 창 함수로 쿼터를 넣자 플래너가 조인 순서를 바꿔 무작위 계열이 해시 순서 대신 PK 연속 상품을 반환 — 기존 코드의 "CTE 조인이 순서를 보존한다"는 비보장 가정이 드러남. ② explicit order by로 고정하면 조기 종료가 사라져 콜드 26초, 2패스 조기 종료 방식도 조건 증가로 플래너가 c_goods 전체 스캔 해시 조인 선택 → 35초. **해법 = c_feed_page 패턴의 일반화:** 무작위 계열 공통 풀을 "좁은 c_thumb_dims 해시 정렬 상위 1,200 물질화"로 재설계하고, 필요한 카탈로그 속성(썸네일 그래픽 g0·성별·색 코드·시즌 연도·피드 자격)을 c_thumb_dims에 물질화(스냅숏이라 정적) — 플랜 무관 결정적, 프로브 유계, 시드별 c_goods 랜덤 IO(~1.2초) 제거. 최종 실측: 웜 1.1~2.1초(기존 범위), 신선 비율 세션 50%·부분 25%·반대 33%·다양성 21%(페이지 합 ~29%, 기저 ~25% — 소폭 가산·쏠림 없음), 축 정확성·결정성·자격 누출 0·콜드스타트 100% 다양성 유지. **계측 버전 태그**: 프론트 `MODEL_VER = "siglip2-base+cls2+mix2"` — 모든 이벤트에 실려 배포 전후 지표 분리. 프론트 check·vitest 78개 통과.
- **5단계 완료 (2026-08-16):** **입력 방어** `20260816230000` — 배열 아닌 jsonb→빈 배열, 앵커는 앞 200개 중 g 숫자(≤12자리)·w 유한 양수(정규식)만 채택·w [0.01,1000] 클램프, 제외 목록 NULL 제거·600 상한. 비정상 입력 배터리(비배열·NaN/Infinity/음수 가중치·NULL 배열·5,000개 제외·1,000개 앵커) 전부 오류 없이 정상 페이지 반환, 정상 분포 회귀 없음. **성능 게이트**(세션 2+장기 3 앵커, 제외 600, 30장): 웜 8회 p50 ≈1.05초·p95 ≈2.4초(악화 없음 ✅), EXPLAIN 이진 인덱스 사용·temp spill 0 ✅, 동시 2요청 각 2.75초+폴백 피드 0.42초 생존 ✅. **동시 4요청은 미통과**(각 16~18초 — work_mem 16MB로 낮춰도 동일, Micro 공유 CPU 포화로 추정. 검증 사용자 1~2명 규모에선 문제없고 초과 시 5초 클라 타임아웃→무작위 폴백으로 강등. 컴퓨트 업그레이드 결정(보류 중)의 근거 수치로 기록). **계측** `personalization_metrics.sql` 신선도 지표를 model_ver별 분리. **Orca 브라우저 E2E**: 피드→블랙 레터링 상품 탭→찜(♥)→하단 유사 그리드(앵커 패밀리 정렬·표 이미지 없음)→피드 스크롤 4페이지 — 서버 이벤트에 `siglip2-base+cls2+mix2` 태그로 random 1페이지+personalized(세션 8·부분 4·반대 3·다양성 15) 기록, tap·wish 귀속 확인. 최종 게이트: `npm run check`·vitest 78·pytest 45 통과. develop 대상 PR 오픈. `20260816180000` 마이그레이션 적용. ① 가중치 역전 — 재정렬 점수를 `(<#>)/w → (<#>)×w`로 교정. 실측: w=5·w=1 두 앵커 프로필에서 세션 슬롯 6장의 소속이 **0/6 → 6/6**(w=5 앵커 계열)으로 뒤집힘. ② 전 경로 자격 — `c_thumb_dims.card_ok`(썸네일 slot0 유형 기준, 현행 2류 제외 = 5개 상품 false)를 물질화하고 `c_feed_page`·믹스 shuffled(부분·반대·다양성 공통)에 적용. 페이지 미달 없음(100/100), 믹스·피드 응답에서 2류 썸네일 누출 0건. 성능: 웜 0.96~1.33초(기존 1.0~2.1초 대비 회귀 없음), 유형 분포 12/6/4/3/5·페이지 결정성 유지. 뷰에 필터를 안 넣은 이유: c_feed_page가 좁은 c_thumb_dims에서 페이지를 먼저 뽑아 뷰 조인 시 필터하면 페이지가 미달됨 — 물질화가 페이지 충족·성능 모두 안전. 브랜치 `feature/personalization-refinement`.

## 미정 (지어내지 않고 남겨둔 것)

- 가중치 교정의 구체 식, 전 경로 필터의 구현 위치(뷰 vs 슬롯별) — 0단계 성능 실측으로 결정
- 홀드아웃 표본 크기·합격선 수치 — 1단계에서 확정해 기록
- 공식 색 코드 사전 확보 방법 — 3단계에서 확인, 실패 시 대체 경로 기록
- 부분 일치 보조 조건·반대 축 결합 방식·신선도 가중 방식과 상수 — 3·4단계 실측으로 결정
- 알고리즘 버전 태그의 값 체계 — 4단계 구현에서 결정
