# 플로팅 검색 구현 계획

- 설계 문서: [`docs/superpowers/specs/2026-08-16-floating-search-design.md`](../superpowers/specs/2026-08-16-floating-search-design.md) (Codex 리뷰 반영본 — 세부 결정은 모두 이 문서를 따른다)
- 브랜치: `feature/floating-search` (→ `develop` PR)
- 작성일: 2026-08-16

## 목표

aTee 첫 페이지에 하단 플로팅 검색창을 추가한다. 검색어를 제출하면 같은 페이지의 모자이크 피드가 검색 결과로 교체되고, 스크롤을 내리면 검색창이 원형 버튼으로 축소되며, 축소 상태에서 탭하면 다시 펼쳐진다.

## 전제

- 백엔드 검색은 새 Supabase RPC로 만들며, 기본 피드와 동일한 노출 자격(card_ok)을 적용한다.
- 검색 결과 정렬은 상품번호 순(의도적 MVP 선택 — 설계 문서 §1 참고).
- 미정: 2음절 검색어("반팔")의 실측 성능 — 1단계에서 측정해 수용 불가면 그때 대응을 정한다.

## 단계

### 1단계 — 백엔드 검색 RPC (핵심 경로의 시작)

검색 RPC 마이그레이션을 작성해 Supabase에 적용한다. 설계 문서 §1의 요건(노출 자격 동일 적용, 검색어 정규화·특수문자 처리·크기 상한 등 입력 방어, 실행 권한 제한, 텍스트 검색 인덱스와 확장 활성화, 상품번호 keyset 페이징)을 모두 포함한다.

- **완료 기준 1**: DB에 직접 RPC를 호출하면 — "나이키"로 브랜드 매칭 상품이, "나이키 반팔"로 두 단어 모두 포함된 상품이, 커서를 이어 넘기면 중복·누락 없는 다음 페이지가 반환된다.
- **완료 기준 2**: 기본 피드에서 제외되는(card_ok 미달) 상품이 검색 결과에 나타나지 않는다.
- **완료 기준 3**: `%`만 제출하거나 과도한 길이·크기를 제출하면 전체 스캔이 아니라 안전한 거부/제한 동작이 확인된다.
- **완료 기준 4**: 대표 검색어(2음절·3음절·영문 브랜드·복합어)의 실행 계획·소요 시간을 측정해 기록하고, 모두 anon 8초 한도 안에 든다. (미달 시: 대응 방안을 정한 뒤 다음 단계 진행)

### 2단계 — 프론트 검색 데이터 계층·결과 훅

검색 API 래퍼와 검색 결과 무한 스크롤 훅을 만든다. 설계 문서 §2의 요건(입력 중 검색어와 제출된 검색어 분리, 요청 세대 번호로 늦은 응답·해제 후 응답 폐기, 새 검색 시 커서·목록 초기화, 상세·비활성 시 로드 중지)을 포함한다. 기존 카드 표시 타입은 재사용 가능한 위치로 옮긴다.

- **완료 기준**: 프론트 테스트 실행 시 — API 매핑, 검색어 상태 분리, "A 제출 후 B 제출 → 늦게 온 A 응답 폐기", "요청 중 해제 → 폐기", 페이징 이어붙기 시나리오가 모두 통과한다. 기존 테스트도 전부 통과한다.

### 3단계 — 검색창(확장 상태)과 피드 교체 (첫 화면에서 눈으로 확인)

하단 플로팅 검색창(확장 상태만)을 첫 페이지에 붙이고, 제출 시 기본 피드가 검색 결과 그리드로 교체되게 한다. 설계 문서 §2의 모드 전환 요건(기본 피드는 DOM 대신 데이터·스크롤 위치만 보존, 두 피드의 명시적 로드 중지 조건, 스크롤 전이 4단계)을 포함한다.

- **완료 기준 1**: 브라우저에서 첫 페이지를 열면 하단에 다크 테마와 어울리는 검색창이 보이고, 검색어를 입력해 엔터를 치면 피드가 해당 검색 결과로 바뀌며 상단에 검색어 라벨이 보인다.
- **완료 기준 2**: 검색 결과를 아래로 스크롤하면 다음 페이지가 이어서 로드된다.
- **완료 기준 3**: 깊게 스크롤한 뒤 검색했다가 X(지우기)로 해제하면, 원래 피드가 이전 스크롤 위치 그대로 복원돼 보인다.

### 4단계 — 검색 결과에서 상세 연동

검색 결과 카드를 탭하면 기존 상세 화면이 열리고, 뒤로가기·유사 탐색 체인이 기본 피드와 동일하게 동작하게 한다. 검색 노출은 개인화 계측에 기록하지 않는다.

- **완료 기준 1**: 검색 결과 카드를 탭하면 상세가 열리고, 상세 하단 유사 상품으로 체인 탐색 후 뒤로가기를 반복하면 검색 결과 화면으로 정확히 돌아온다.
- **완료 기준 2**: 상세가 열려 있는 동안 검색창이 보이지 않고, 닫으면 입력값이 유지된 채 다시 보인다.
- **완료 기준 3**: 검색 결과 노출은 이벤트 로그에 기록되지 않고, 상세 열기·찜은 기존대로 기록된다(브라우저 확인).

### 5단계 — 축소/확장 애니메이션

스크롤 방향에 따라 검색창이 원형 버튼으로 줄어들고 다시 펼쳐지는 전환을 만든다. 외부 라이브러리 없이 CSS 전환으로 처리하고, 프로그램적 스크롤(복원·상단 이동)은 판정에서 제외한다.

- **완료 기준 1**: 피드를 아래로 스크롤하면 검색창이 부드럽게 원형 버튼으로 줄어들고, 위로 스크롤하면 다시 펼쳐진다.
- **완료 기준 2**: 축소된 버튼을 탭하면 검색창이 펼쳐지며 입력란에 포커스가 잡히고, 축소 전 입력값이 남아 있다.
- **완료 기준 3**: 검색 해제로 스크롤이 복원되는 순간 검색창이 오작동(불필요한 축소/확장)하지 않는다.

### 6단계 — 엣지 케이스·겹침 처리

결과 0건 안내, 오류 시 수동 재시도, 동의 배너와의 겹침, 하단 안전 영역·피드 패딩을 마무리한다.

- **완료 기준 1**: 없는 검색어를 제출하면 "결과 없음" 안내와 지우기 버튼이 보이고, 지우면 원래 피드로 돌아온다.
- **완료 기준 2**: 네트워크를 끊고 검색하면 자동 재시도 반복 없이 오류 안내와 재시도 버튼이 보이고, 재시도 버튼으로 복구된다.
- **완료 기준 3**: 최초 방문(동의 배너 표시) 상태에서 검색창과 배너가 겹치지 않고 둘 다 조작 가능하다.

### 7단계 — 최종 검증·PR

품질 게이트를 모두 돌리고 실제 브라우저로 전체 흐름을 검증한 뒤 PR을 만든다.

- **완료 기준 1**: `npm run check`와 `npm run test`(프론트), `pytest`(백엔드)가 모두 통과한다.
- **완료 기준 2**: Orca 브라우저(iOS 뷰포트)에서 — 검색 → 결과 스크롤 → 상세 체인 → 복귀 → 해제 → 스크롤 복원 → 축소/확장 애니메이션까지 한 흐름으로 확인된다.
- **완료 기준 3**: `develop` 대상 PR이 열리고 CI가 초록불이다.

## 진행 기록

- **1단계 완료 (2026-08-16):** `c_search_page` RPC 적용 (`20260816240000` 마이그레이션).
  - **발견 — pg_trgm 폐기:** 설계대로 trgm GIN을 만들었으나, ilike 선택도 추정이 패턴 내용을 반영하지 못해 플래너가 흔한 단어에도 전체 GIN 스캔을 골랐다 ("반팔" 5.5~27s, "mlb" 41s — anon 8초 한도 초과). `plan_cache_mode=force_custom_plan`으로도 해결 안 됨. **대신 노출 자격을 만족하는 상품만 담은 좁은 물질화 테이블 `c_search_text`(226,304행, 28MB, RLS 기본 거부)를 전체 스캔**하는 방식으로 교체 — 어떤 검색어든 최악이 유계돼 예측 가능.
  - **실측(교체 후):** 반팔 70→18ms · 후드 21ms · 나이키 298ms · mlb 182ms · 나이키 반팔 251ms · 오버핏 그래픽 97ms · 늑대 123ms · 무의미어 96ms — 전부 8초 한도 내. 2음절 성능 문제 해소(미정 항목 해소).
  - **검증:** "나이키" 브랜드 매칭·"나이키 반팔" 전 단어 매칭 ✅ · keyset 2페이지 중복 0·상위 60과 일치 ✅ · card_ok=false 양성 대조 2건(3167851, 3740164) 직전 커서에서 미노출 ✅ · `%`는 리터럴 매칭(286개 % 포함 상품)·빈/공백/NULL/500자 검색어 0건·p_size 100000→60 클램프 ✅ · anon RPC 실행 가능, `c_search_text` 직접 조회는 RLS 차단 ✅.
  - **저장 공간:** +28MB (여유 71MB 중). c_goods 재수집·card_ok 재분류 시 마이그레이션 재실행으로 재생성 필요(파일 주석에 명시).
- **2단계 완료 (2026-08-16):** 검색 데이터 계층·훅 + 테스트 13개.
  - `search-api.ts`(기존 `mapFeedDto` 재사용), `use-search-state.ts`(입력/제출 검색어 분리, 빈 제출 무시, clear), `use-search-feed.ts`(keyset 무한 스크롤).
  - **경합 방어 구현:** 결과·진행 상태가 자신을 만든 검색어를 기억하고 현재 검색어와 다르면 응답 폐기·파생 무시 — 별도 리셋 effect가 필요 없어 엄격 린트(`set-state-in-effect`, 렌더 중 ref 변경 금지)와도 호환. 오류는 자동 재시도 없이 `retry()`로만 해제(오류 중 센티널 교차도 무시).
  - `FeedCardViewData`·`ImpressionDomInfo`를 `card-view-data.ts`로 분리(기존 경로 재노출로 호출부 무변경).
  - 테스트: A→B 늦은 A 폐기 · 요청 중 해제 폐기 · 커서 이어붙기 · paused · isEmpty · retry. 전체 91개 통과, `npm run check` 통과.
- **3단계 완료 (2026-08-16):** 하단 플로팅 검색창(확장 상태) + 검색 모드 피드 교체.
  - `floating-search.tsx`(알약형, safe-area, 상세 열림 시 hidden만), `search-results.tsx`(같은 FeedGrid 재사용, onImpress 미전달, 라벨·0건·오류 표시), `use-search-scroll.ts`(전이), MosaicFeed 통합(기본 피드 paused=상세∨검색, 검색 피드 paused=상세).
  - **발견 — 스크롤 저장 시점 버그:** layout effect에서 저장하면 같은 커밋에서 피드 DOM이 먼저 짧아져 scrollY가 이미 클램프된 값(4000→479)으로 저장됨. 저장을 **제출 이벤트 시점**(DOM 교체 전)으로 옮겨 해결.
  - Orca 브라우저 검증: '나이키'/'후드'/'반팔' 검색 → 결과 라벨·카드 30장 표시(`c_search_page 200`) → 결과 스크롤 시 30→60장 페이징 → 해제 시 피드 복귀 + 스크롤 정확 복원(7267→0→7267).
- **4단계 완료 (2026-08-16):** 상세 연동 — 추가 코드 없이 3단계 배선(공유 상세 스택)으로 충족, 브라우저 검증.
  - 검색 결과 카드 탭 → 상세 열림 + 검색창 숨김(opacity-0) → 상세 하단 유사 상품으로 체인(2층) → 뒤로가기 2회 → 검색 결과 정확 복귀 + 입력값('나이키') 유지 + 검색창 재표시.
  - 계측 분리: 노출 기록은 `use-feed-view-model.onImpress`에만 있고 검색 그리드는 미전달(+기본 피드는 검색 중 paused라 유령 노출 차단). 상세 열기·찜은 기존 중앙 경로(use-detail-state·use-wishlist) 그대로.
- **5단계 완료 (2026-08-16):** 축소/확장 애니메이션 (`use-search-collapse` + FloatingSearch 축소 UI, CSS 전환만).
  - 판정: 같은 방향 누적 60px 넘으면 전환, 상단 80px 이내는 항상 확장, 프로그램적 스크롤(useSearchScroll이 suppress 시각 공유)은 방향 판정 제외.
  - **발견 — 축소 잔상 버그:** 깊이 스크롤(축소 상태)에서 검색을 제출하면 결과 상단인데 축소가 남음. "상단 = 항상 확장" 규칙을 억제 구간에도 적용해 해결(테스트 추가).
  - 브라우저 검증: 아래 스크롤 → 원형 축소 · 위 스크롤 → 재확장 · 축소 버튼 탭 → 확장+포커스+입력값 유지 · 제출 → 결과 상단 확장 · 해제 → 위치 정확 복원 + 오작동 없음. 테스트 96개(check 포함) 통과.
- **6단계 완료 (2026-08-16):** 엣지 케이스·배너 겹침.
  - 배너 표시 여부를 `shared/consent-notice-store.ts`로 공유(feature 간 직접 import 회피) — 배너가 보이면 검색창이 그 위(bottom 9rem)로 물러나고 닫히면 내려온다.
  - 브라우저 검증: 0건 검색("결과가 없어요"+지우기→피드 복귀) · fetch 차단 시 오류 표시+자동 재시도 없음+수동 재시도로 복구(30장) · 배너와 검색창 겹침 없음(rect 교차 검사 false), 배너 닫으면 검색창 하강(614→742). 테스트 97개 통과.
- **7단계 완료 (2026-08-16):** 최종 검증 — `npm run check`·`npm run test`(97개)·`npm run build`·`pytest`(45 passed, 9 skipped) 전부 통과. Orca 브라우저 연속 E2E: 피드 스크롤 축소 → 축소 버튼 탭 확장 → '나이키 반팔' 검색 → 결과 상단 확장 표시 → 상세 열고 복귀 → 해제 시 피드 위치 정확 복원(2000→2000). ('나이키 반팔'은 전체 3건이라 페이징 미발생이 정상 — 페이징은 '나이키'/'반팔' 30→60장으로 별도 검증.) develop 대상 PR 생성.
